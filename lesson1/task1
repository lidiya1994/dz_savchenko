#Необходимо написать программу, которая требует, пользователя ввести два целых числа
#строку и одно дробное число,далее выводит на экран строкуи сумму двух целых и дробного чисел
a = int(input("Введите первое целое число:"))
b = int(input("Введите второе целое число:"))
c = input("Введите строку:")
d = float(input("Введите дробное число:"))

#сумма целых чисел и дробных
t = a+b+d
print(c)
print(t)

name=input("Введение имени:")
surname=input("Введение фамилии:")
patronymic=input("Введение отчества:")
age=int(input("Введите возраст"))
city=input("Введите город:")

print (surname, name, patronymic)
print(age)
print(city)

#Создать 4 переменных. С типом данных целых чисел, чисел с плавающей точкой, логическим и строковым.
#Преобразовать числа в строку.

a1 = 1
a2 = 2.0
a3 = True
a4 = "Hello"

a1 = str(a1)
a2 = str(a2)


#Импортируется модуль math, содержащий различные математические функции

import math

#функция input()возвращает строку
AB = input ("длина первого катета:")
AC = input ("длина второго катета:")

# строки переводятся в вещественные числа
AB = float(AB)
AC = float(AC)

# находим гипотенузу по теореме пифагора:
# "сумма квадратов катетов
#равна квадрату гипотенузы"
#функция sqrt()из модуля math
#извлекаем квадратный корень.
#оператор возвращает в квадрат.
BC =math. sqrt(AB **2 +AC **2)

#площадь прямоугольного треугольника
#равна половине площади
#соответствующего прямоугольника
S =(AB*AC)/2
#периметр находиться как сумма всех сторон
P=AB+AC+BC
print("Периметр =,P")